# LightDevCoder/skills v0.1.4 发布收据

状态：`PENDING RELEASE GATES` — 只有 release gates 全部通过后才完成本收据：
`light-kanban-worker` 准入 `PASS`、完整测试套件 `PASS`、catalog 与安装文档同步、
针对已发布 tag 的 fresh-install 验证。

v0.1.4 范围：第一方 `light-kanban-worker` Skill、scheduled Light-Kanban
worker 工作流、版本/文档同步清理、安装验证。
