# v0.1.6 host discovery verification

[中文记录](DISCOVERY_VERIFICATION.zh-CN.md)

`PASS` (CLI `1.5.23`) — after installing from the v0.1.6 tag in fresh destinations, `npx --yes skills list` exits 0 and discovers the installed `kb-init` package (and the whole nine-package collection for the whole-collection form) without the source checkout. Exact destination classes are recorded in [INSTALLATION_VERIFICATION.md](INSTALLATION_VERIFICATION.md).
