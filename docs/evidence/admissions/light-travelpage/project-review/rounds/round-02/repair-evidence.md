# Round 2 repairs

Server success is authoritative even if journal cleanup fails; stale stored receipt can replay safely. 401/403 retain the exact uncertain request. Focused tests reproduce both edge cases.

Ledger recovery now awaits the confirmed snapshot. Newer form input retains its DOM, and a dirty-render marker ensures a refresh after editor closure renders the remote change. Unchanged recovery renders the fresh snapshot immediately. Added member-specific regression tests.

21 Node tests passed, including 5 Happy DOM interaction tests. Actual D1 all-collection, simultaneous CAS, idempotency, authorization and atomic rejection checks passed against Cloudflare. Browser mobile evidence remains outstanding while Mac is locked.
