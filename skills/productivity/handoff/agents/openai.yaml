interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to compact the current conversation into a handoff document for the next agent."

policy:
  allow_implicit_invocation: false
