interface:
  display_name: "Wizard"
  short_description: "Generate an interactive setup wizard"
  default_prompt: "Use $wizard to generate an interactive bash wizard that walks a human through steps only they can perform."

policy:
  allow_implicit_invocation: true
