{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/LightDevCoder/agent-config/main/schemas/validation.schema.json",
  "title": "ValidationResult",
  "description": "Canonical schema for post-apply host configuration validation.",
  "type": "object",
  "required": [
    "valid",
    "workspace",
    "message"
  ],
  "properties": {
    "valid": {
      "type": "boolean",
      "description": "Whether actual host state matches expected configuration."
    },
    "workspace": {
      "type": "string",
      "description": "Workspace directory path checked."
    },
    "message": {
      "type": "string",
      "description": "Summary of validation result."
    },
    "errors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of discrepancies or validation failures if valid is false."
    },
    "details": {
      "description": "Structured details of matched or mismatched configuration keys."
    }
  },
  "additionalProperties": true
}
