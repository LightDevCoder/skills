interface:
  display_name: "Review Loop"
  short_description: "Lightweight review→findings→repair→re-review convergence engine"
  default_prompt: "Use $review-loop to run a lightweight review→findings→repair→re-review loop by resolving a reviewer and converging until clean or bounded limit."

policy:
  allow_implicit_invocation: true
