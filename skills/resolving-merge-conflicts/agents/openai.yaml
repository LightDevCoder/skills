interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Resolve merge and rebase conflicts"
  default_prompt: "Use $resolving-merge-conflicts when resolving an in-progress git merge or rebase conflict without aborting."

policy:
  allow_implicit_invocation: true
