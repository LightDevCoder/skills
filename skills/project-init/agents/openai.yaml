interface:
  display_name: "Project Init"
  short_description: "Bootstrap stable Light project contracts"
  default_prompt: "Use $project-init to inspect this repository and create its confirmed, idempotent Light project configuration."

policy:
  allow_implicit_invocation: false
