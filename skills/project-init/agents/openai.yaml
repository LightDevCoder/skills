interface:
  display_name: "Project Init"
  short_description: "Initialize software and non-software projects"
  default_prompt: "Use $project-init to inspect this project and prepare a confirmed minimal initialization."

policy:
  allow_implicit_invocation: false
