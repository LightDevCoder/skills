interface:
  display_name: "Project Clarify"
  short_description: "Clarify project decisions from inspected facts in frontier rounds"
  default_prompt: "Use $project-clarify to inspect this project's real materials, resolve the current frontier in rounds, and return a bounded clarification handoff."

policy:
  allow_implicit_invocation: false
