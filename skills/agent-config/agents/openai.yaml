interface:
  display_name: "Agent Config"
  short_description: "Map host capabilities and confirmed profile to execution topology, tier, and effort"
  default_prompt: "Use agent-config to inspect current host capabilities, read confirmed profile tiers, assess task shape, right-size execution tier and actual effort, and choose a safe execution topology."
policy:
  allow_implicit_invocation: true
