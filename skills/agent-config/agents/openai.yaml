interface:
  display_name: "Agent Config"
  short_description: "Map verified host capabilities to a safe execution plan"
  default_prompt: "Use agent-config to map current host evidence and exact work ownership to a safe Controller, Explorer, Implementer, Reviewer, and Merger plan."
policy:
  allow_implicit_invocation: true
